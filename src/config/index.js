/**
 * Central Configuration Loader
 *
 * Reads environment variables and exports a structured config object.
 * All config access should go through this module — never read
 * process.env directly in application code.
 *
 * Implementation: Phase 6
 */

// TODO: Import dotenv and call config()

// TODO: Export config object with sections:
// - server: { port, env }
// - database: { uri }
// - jwt: { secret, expiry }
// - github: { token, webhookSecret }
// - openai: { apiKey, model }
// - semgrep: { appToken }
// - zap: { apiKey, baseUrl }
// - sandbox: { image, timeout, memoryLimit, cpuLimit }
// - logging: { level, dir }
