/**
 * Utility Helpers
 *
 * General-purpose helper functions used across the app.
 *
 * Implementation: Phase 6
 */

// TODO: generateId() — UUID v4 generation for workflow/vuln IDs
// TODO: sleep(ms) — Promise-based delay for backoff
// TODO: formatDuration(ms) — Human-readable duration string
// TODO: sanitizePath(path) — Prevent path traversal in file operations
// TODO: truncate(str, maxLen) — Truncate long strings for logging
